import 'package:flutter/material.dart';

class RegisterScroller extends StatelessWidget {
  const RegisterScroller({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}